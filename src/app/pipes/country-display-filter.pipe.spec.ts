import { CountryDisplayFilterPipe } from './country-display-filter.pipe';

describe('CountryDisplayFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryDisplayFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
